{
  "build": {
    "preview": {
      "android": {
        "buildType": "apk",
        "credentials": {
          "keystore": {
            "keystoreBase64": "${EXPO_ANDROID_KEYSTORE_BASE64}",
            "keystorePassword": "${EXPO_ANDROID_KEYSTORE_PASSWORD}",
            "keyAlias": "${EXPO_ANDROID_KEY_ALIAS}",
            "keyPassword": "${EXPO_ANDROID_KEY_PASSWORD}"
          }
        }
      },
      "ios": {
        "simulator": true
      }
    },
    "production": {
      "android": {
        "buildType": "apk",
        "credentials": {
          "keystore": {
            "keystoreBase64": "${EXPO_ANDROID_KEYSTORE_BASE64}",
            "keystorePassword": "${EXPO_ANDROID_KEYSTORE_PASSWORD}",
            "keyAlias": "${EXPO_ANDROID_KEY_ALIAS}",
            "keyPassword": "${EXPO_ANDROID_KEY_PASSWORD}"
          }
        }
      },
      "ios": {}
    }
  }
}
